---
image: docker.io/nixos/nix:2.24.11

variables:
  NIX_CONFIG: "experimental-features = nix-command flakes"

.nix-cache:
  cache:
    key:
      files:
        - flake.lock
    paths:
      - .nix-cache
  before_script:
    - |
      if [ -d .nix-cache/store ]; then
        echo "Restoring Nix store from cache..."
        cp -rn .nix-cache/store/* /nix/store/ 2>/dev/null || true
        nix-store --load-db < .nix-cache/db.dump
      fi
  after_script:
    - |
      echo "Saving Nix store to cache..."
      mkdir -p .nix-cache/store
      nix-store --dump-db > .nix-cache/db.dump
      nix path-info --derivation --recursive .#devShells.x86_64-linux.default 2>/dev/null \
        | xargs -I{} cp -rn {} .nix-cache/store/ 2>/dev/null || true

stages:
  - build
  - deploy
  - release

build:
  extends: .nix-cache
  stage: build
  script:
    - nix develop --command bash -c "task build DTTR_VERSION=${CI_COMMIT_TAG:-dev} && (cd build/dist && zip -r ../../dttr-${CI_COMMIT_SHORT_SHA}-debug.zip Debug/)"
  artifacts:
    paths:
      - dttr-*-debug.zip
    expire_in: 2 weeks
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

build-components:
  extends: .nix-cache
  stage: build
  script:
    - nix develop --command bash -c "task build DTTR_VERSION=${CI_COMMIT_TAG:-dev} DTTR_COMPONENTS=ON && (cd build/dist && zip -r ../../dttr-${CI_COMMIT_SHORT_SHA}-debug-components.zip Debug-components/)"
  artifacts:
    paths:
      - dttr-*-debug-components.zip
    expire_in: 2 weeks
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

build-release:
  extends: .nix-cache
  stage: build
  script:
    - nix develop --command bash -c "task build-release DTTR_VERSION=${CI_COMMIT_TAG} && cd build/dist && zip -r ../../dttr-${CI_COMMIT_TAG}-debug.zip Debug/ && zip -r ../../dttr-${CI_COMMIT_TAG}-release.zip Release/"
  artifacts:
    paths:
      - dttr-*-debug.zip
      - dttr-*-release.zip
    expire_in: 2 weeks
  rules:
    - if: $CI_COMMIT_TAG

build-release-components:
  extends: .nix-cache
  stage: build
  script:
    - nix develop --command bash -c "task build-release DTTR_VERSION=${CI_COMMIT_TAG} DTTR_COMPONENTS=ON && cd build/dist && zip -r ../../dttr-${CI_COMMIT_TAG}-debug-components.zip Debug-components/ && zip -r ../../dttr-${CI_COMMIT_TAG}-release-components.zip Release-components/"
  artifacts:
    paths:
      - dttr-*-debug-components.zip
      - dttr-*-release-components.zip
    expire_in: 2 weeks
  rules:
    - if: $CI_COMMIT_TAG

pages:
  extends: .nix-cache
  stage: deploy
  script:
    - nix develop --command task generate-docs
    - rm -rf public
    - cp -r docs/html public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  variables:
    GIT_DEPTH: 0
  script:
    - |
      PREV_TAG=$(git describe --tags --abbrev=0 HEAD^)
      git log --first-parent --no-merges --format="- %s (%h)" "${PREV_TAG}..HEAD" > changelog.md
  needs:
    - job: build-release
      artifacts: true
    - job: build-release-components
      artifacts: true
  release:
    tag_name: $CI_COMMIT_TAG
    description: ./changelog.md
    assets:
      links:
        - name: "dttr-${CI_COMMIT_TAG}-release.zip"
          url: "${CI_PROJECT_URL}/-/jobs/${CI_JOB_ID}/artifacts/raw/dttr-${CI_COMMIT_TAG}-release.zip"
        - name: "dttr-${CI_COMMIT_TAG}-debug.zip"
          url: "${CI_PROJECT_URL}/-/jobs/${CI_JOB_ID}/artifacts/raw/dttr-${CI_COMMIT_TAG}-debug.zip"
        - name: "dttr-${CI_COMMIT_TAG}-release-components.zip"
          url: "${CI_PROJECT_URL}/-/jobs/${CI_JOB_ID}/artifacts/raw/dttr-${CI_COMMIT_TAG}-release-components.zip"
        - name: "dttr-${CI_COMMIT_TAG}-debug-components.zip"
          url: "${CI_PROJECT_URL}/-/jobs/${CI_JOB_ID}/artifacts/raw/dttr-${CI_COMMIT_TAG}-debug-components.zip"
  artifacts:
    paths:
      - dttr-*-release.zip
      - dttr-*-debug.zip
      - dttr-*-release-components.zip
      - dttr-*-debug-components.zip
  rules:
    - if: $CI_COMMIT_TAG
